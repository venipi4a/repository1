#include <stdio.h>

int main()
{
    int xa, ya, xb, yb ,x ,y;
    printf("xa = ");
    scanf("%d", &xa);
    printf("ya ");
    scanf("%d", &ya);
    printf("xb = ");
    scanf("%d", &xb);
    printf("yb = ");
    scanf("%d", &yb);
    x = xb + xy;
    y = ya + yb
    printf("%d + %d = %d\n", a, b, c);
    if(x <= y)
    {
        printf(y - x)
    }
    if(y <= x)
    {
        printf(x - y)
    }
}